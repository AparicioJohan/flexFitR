# Generic for inverse prediction

Generic for inverse prediction

## Usage

``` r
inverse_predict(object, ...)
```

## Arguments

- object:

  An object for which to compute the inverse prediction.

- ...:

  Additional arguments passed to methods.
